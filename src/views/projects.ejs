<%- include('partials/header') %>

  <main id="main-content">
    <div class="page-content">
      <div class="container">
        <div class="page-header">
          <h1>
            <%= title %>
          </h1>
          <p>Upcoming service projects you can join:</p>
          <a href="/new-project" class="btn btn-primary">Add New Project</a>
        </div>

        <ul class="projects-list" role="list">
          <% projects.forEach(function(project) { %>
            <li class="card project-card">
              <h3><a href="/project/<%= project.service_project_id %>">
                  <%= project.title %>
                </a></h3>
              <p><strong>Organization:</strong>
                <a href="/organization/<%= project.organization_id %>">
                  <%= project.organization_name %>
                </a>
              </p>
              <p><strong>Description:</strong>
                <%= project.description %>
              </p>
              <p><strong>Location:</strong>
                <%= project.location %>
              </p>
              <p><strong>Date:</strong>
                <%= new Date(project.project_date).toLocaleDateString() %>
              </p>
            </li>
            <% }); %>

        </ul>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>