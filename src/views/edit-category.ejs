<%- include('partials/header') %>
  <main id="main-content">
    <div class="page-content">
      <div class="container">
        <div class="page-header">
          <h1>
            <%= title %>
          </h1>
          <p>Update the name for this category.</p>
        </div>

        <form action="/edit-category/<%= category.category_id %>" method="POST" class="card form-card">
          <div class="form-group">
            <label for="name">Category Name</label>
            <input type="text" id="name" name="name" maxlength="100" required value="<%= category.name %>" />
          </div>

          <div class="form-actions">
            <button type="submit" class="btn btn-primary">Update Category</button>
            <a href="/category/<%= category.category_id %>" class="btn btn-secondary">Cancel</a>
          </div>
        </form>
      </div>
    </div>
  </main>
  <%- include('partials/footer') %>